# Archive

Code kept for provenance, not part of the released system. Nothing here is
imported by the training or evaluation paths, and none of it produces any
number in the paper.

- `diffusion.py` — DiT/DDPM latent-generation experiment (372 lines). It has
  no released training entry point and no other tracked file references its
  symbols. Moved here rather than deleted so its history stays reachable:
  `git log --follow archive/diffusion.py`.
